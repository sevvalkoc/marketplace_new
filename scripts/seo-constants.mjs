// Kept in sync with SITE_URL in src/app/lib/useSEO.ts — duplicated only
// because build scripts (plain Node) and the app bundle (Vite/TS) aren't
// sharing a module graph. If the domain ever changes, update both.
export const SITE_URL = 'https://studiomarche.co.uk';
